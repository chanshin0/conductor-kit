{
  "name": "@conductor-kit/agent-codex",
  "version": "0.0.0",
  "description": "Codex CLI adapter for conductor-kit (AGENTS.md + prompts). Installed into host repos via conductor-install.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'static adapter, nothing to build'",
    "typecheck": "echo 'no ts'",
    "test": "echo 'no tests'",
    "lint": "echo 'no lint'"
  }
}
