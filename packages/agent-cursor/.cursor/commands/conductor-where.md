# /conductor-where

Read-only state snapshot.

Run:

```bash
conductor where --json --agent "Cursor"
```

Render a 3–5 line summary from `data`. If `next_action` references a CLI
command, surface it verbatim — the user can click-run it.

Do not modify files.
