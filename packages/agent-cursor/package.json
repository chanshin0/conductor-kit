{
  "name": "@conductor-kit/agent-cursor",
  "version": "0.0.0",
  "description": "Cursor adapter for conductor-kit (.cursor/rules + commands). Installed into host repos via conductor-install.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'static adapter, nothing to build'",
    "typecheck": "echo 'no ts'",
    "test": "echo 'no tests'",
    "lint": "echo 'no lint'"
  }
}
