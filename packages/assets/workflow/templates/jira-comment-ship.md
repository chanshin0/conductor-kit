🚀 MR 등록 — {ISSUE_KEY}

• MR: {MR_URL}
• 브랜치: {BRANCH} → {TARGET_BRANCH}
• 요약: {SUBJECT}

검증: lint / type-check / test 통과
{UI_NOTE}

리뷰 & 머지 후 conductor land {ISSUE_KEY}

---

Generated via `conductor {COMMAND}` — agent {AGENT} · conductor-kit v{CLI_VERSION} · user {USER}
